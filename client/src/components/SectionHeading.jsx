import { motion } from "framer-motion";
export const SectionHeading = ({ eyebrow, title, copy, align = "center", light = false }) => <motion.div initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.25 }} transition={{ duration: 0.55 }} className={`mb-12 ${align === "center" ? "mx-auto text-center" : ""} max-w-2xl`}><p className="mb-4 text-xs font-bold uppercase tracking-[.2em] text-[#b89225]">{eyebrow}</p><h2 className={`font-display text-4xl leading-tight md:text-5xl ${light ? "text-white" : "text-[#142235]"}`}>{title}</h2>{copy && <p className={`mt-5 text-base leading-7 ${light ? "text-slate-300" : "text-slate-600"}`}>{copy}</p>}</motion.div>;
