import { useState } from "react";
import { HiBars3, HiBell, HiChevronDown, HiOutlineArrowRightOnRectangle } from "react-icons/hi2";
import { notifications } from "../../data/adminDashboard";

export const AdminHeader = ({ admin, onMenu, onLogout }) => {
  const [panel, setPanel] = useState(null);
  return <header className="flex items-center justify-between border-b border-[#e9e3d8] bg-white px-5 py-4 md:px-8"><div className="flex items-center gap-4"><button onClick={onMenu} className="rounded-lg p-2 text-xl hover:bg-[#faf9f6] lg:hidden" aria-label="Open navigation"><HiBars3 /></button><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#b89225]">Monday, 11 August</p><h1 className="font-display mt-1 text-2xl md:text-3xl">Good morning, {admin?.name?.split(" ")[0] || "Admin"}.</h1></div></div><div className="flex items-center gap-2 md:gap-4"><div className="relative"><button onClick={() => setPanel(panel === "notifications" ? null : "notifications")} className="relative rounded-xl p-3 text-xl text-slate-700 hover:bg-[#faf9f6]" aria-label="Notifications"><HiBell /><span className="absolute right-2 top-2 h-2 w-2 rounded-full bg-[#d4af37]" /></button>{panel === "notifications" && <div className="absolute right-0 top-14 z-30 w-[min(22rem,90vw)] rounded-2xl border border-[#e9e3d8] bg-white p-3 shadow-xl"><p className="px-3 py-2 text-xs font-bold uppercase tracking-[.16em] text-[#b89225]">Recent notifications</p>{notifications.map(item => <button className="w-full rounded-xl p-3 text-left hover:bg-[#faf9f6]" key={item.title} onClick={() => setPanel(null)}><p className="text-sm font-semibold">{item.title}</p><p className="mt-1 text-xs text-slate-500">{item.detail} · {item.time}</p></button>)}</div>}</div><div className="relative"><button onClick={() => setPanel(panel === "profile" ? null : "profile")} className="flex items-center gap-2 rounded-xl p-1.5 pr-2 text-left hover:bg-[#faf9f6]"><span className="grid h-9 w-9 place-items-center rounded-full bg-[#142235] text-sm font-bold text-[#d4af37]">{admin?.name?.slice(0, 1)}</span><span className="hidden md:block"><span className="block text-sm font-semibold leading-4">{admin?.name}</span><span className="block text-xs text-slate-500">Administrator</span></span><HiChevronDown className="hidden text-sm md:block" /></button>{panel === "profile" && <div className="absolute right-0 top-14 z-30 w-52 rounded-2xl border border-[#e9e3d8] bg-white p-2 shadow-xl"><p className="px-3 py-2 text-xs text-slate-500">{admin?.email}</p><button onClick={onLogout} className="flex w-full items-center gap-2 rounded-xl px-3 py-3 text-left text-sm font-semibold text-red-700 hover:bg-red-50"><HiOutlineArrowRightOnRectangle /> Sign out</button></div>}</div></div></header>;
};
